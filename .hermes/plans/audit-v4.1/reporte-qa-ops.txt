{"rol":"QA / OPS-PRODUCCIÓN — auditoría de escenarios de fallo del pipeline v4.1 (repo /mnt/data2/projects/jobhunt, rama feature/canal-market-score, cambios sin commitear; servicio systemd user jobhunt corriendo PID 516441 desde 03:20:14 UTC con el código actual: fuentes mttimeadas ≤03:19, .venv/bin/python -m jobhunt watch bajo xvfb-run, Restart=on-failure RestartSec=15)","veredicto":"rechazado","hallazgos":[{"id":"OPS-1","severidad":"P1","titulo":"scan_log: el INSERT se ejecuta ANTES del pipeline con contadores en 0 — las columnas v4.1 (C5) no capturan nada del barrido, siempre valen 0","linea":"cli.py:209, 286-291, 340-342, 366; db.py:104-118","evidencia":"cli.py:209 inicializa lots_done=ia_failures=breaker_trips=channel_posts=0; cli.py:286-291 hace INSERT INTO scan_log(...) con esos valores y commit; los incrementos ocurren DESPUÉS: cli.py:340-342 (lots_done/ia_failures/breaker_trips tras consume_lote) y cli.py:366 (channel_posts). Si el pipeline crashea a mitad (p.ej. rescore_ids en cli.py:356 no tiene try/except en el loop de lotes), la excepción sube a cmd_run (cli.py:405-410) y la fila ya insertada queda con contadores 0 aunque se hayan procesado 6 lotes. Verificado en DB real (SELECT mode=ro): la tabla scan_log de data/ofertas.sqlite ni siquiera tiene aún las columnas lots_done/ia_failures/breaker_trips/channel_posts (.schema devuelve solo id/ts/total_seen/new_count/sources_summary) — la migración db.py:115-118 corre en init_db, que solo se ejecuta al primer cmd_run del daemon nuevo (el daemon 03:20:14 no barre al arrancar, bot.py:1389-1393), o sea que ni siquiera existen todavía en producción.","recomendacion":"Mover el INSERT de scan_log al final de cmd_run (después del loop de lotes, con los contadores reales), o insertar al inicio y hacer UPDATE final con los totales. Añadir try/except alrededor de rescore_ids/enrich_pending por lote para que un fallo no aborte el barrido con contadores en 0."},{"id":"OPS-2","severidad":"P1","titulo":"Guardas C1/C2 solo protegen el barrido manual /search: el barrido AGENDADO (sweep, el camino principal cada 4h) nunca setea _SEARCH_STATE['running'], así que el batch IA nocturno y los digests sí compiten con un scan en curso","linea":"bot.py:1317-1329, 519/576-577, 1198-1200, 1227, 1419-1422","evidencia":"_run_search_async (/search manual) sí setea running=True en bot.py:519 y lo libera en finally bot.py:576-577 (crash-safe, sin flag pegado). Pero sweep() (bot.py:1317-1329), el que lanza el cron de las horas agendadas (bot.py:1419-1422), llama _do_sweep directamente SIN tocar _SEARCH_STATE. Consecuencias: (1) _ia_sweep_maybe (bot.py:1198) y _digests_maybe (bot.py:1227) ven running=False durante un barrido agendado de horas → el digest diario (21:00 UTC, tras el barrido de las 20:00) puede leer el pool a medio procesar exactamente el escenario C2; (2) el batch IA de las 03:00 puede arrancar en paralelo con un scan largo → dos escritores (el patrón conexión-única del main se rompe: run_ia_batch abre su propio conn en bot.py:1019). Además el check inverso pierde barridos: si el barrido de las 04:00 arranca mientras el batch IA sigue vivo, sweep() ve _op_busy()=='ia' y retorna (bot.py:1320-1322), pero last_sweep_key ya fue consumido en bot.py:1420 → ese barrido se pierde, no se pospone. Journal real: 'batch IA falló: database is locked' a las 00:37:03 (PID 379248) es evidencia de esta clase de colisión.","recomendacion":"Setear _SEARCH_STATE.update(running=True, t0=...) (o un flag propio) en sweep() con try/finally, igual que _run_search_async; y no consumir last_sweep_key antes de que el sweep real haya decidido ejecutarse (moverlo dentro del thread tras pasar el check de busy)."},{"id":"OPS-3","severidad":"P1","titulo":"Breaker B5 protege la API pero NO el wall-clock: tras el trip el lote quema su deadline completo (~121 min con 40/conc 2) esperando resultados que nunca llegarán; 7 lotes = ~14 h de barrido muerto y los barridos de 4h siguientes se apilan bloqueados en _sweep_lock","linea":"cli.py:108-117, 126-136, 337; bot.py:1302, 1419-1422","evidencia":"consume_lote solo termina por conteo (recibidos==len(lote), cli.py:108) o deadline (cli.py:109-113). Tras breaker_trips → stop_event.set() (cli.py:132), worker_ia sale solo ANTES de tomar un job nuevo (cli.py:70-74) — correcto—, pero los jobs restantes del work_q jamás se procesan → no llegan resultados → el main gira en get(timeout)/Empty (cli.py:114-117) hasta vencer el deadline: max(300, 40/2×242×1.5)=7260s=121 min por lote (calculado: 7 lotes = 14.1 h). El worker que ya tiene un job en mano puede colgar hasta ~242s extra (2 intentos × timeout 120s + sleep, retries=1 en config.py:311-312), acotado por el drain de 120s solo para resultados en vuelo (cli.py:151-157). Detalle menor: la ventana B5 solo apila resultados 'rate' (cli.py:126-128) y la limpia con 1 éxito (cli.py:135-136), no es 'últimos 20 resultados' literales de la spec — dispara antes, no es dañino. Mientras el barrido de 14 h vive, los barridos agendados siguientes se lanzan igual (bot.py:1419-1422, _SEARCH_STATE sigue False) y cada thread queda BLOQUEADO en _sweep_lock dentro de _do_sweep (bot.py:1302) — se acumulan threads vivos esperando horas. No hay guarda de solapamiento real.","recomendacion":"Al trip del breaker, acortar el deadline del lote (p.ej. salir por conteo imposible → cortar a deadline=min(deadline, t0_trip+gracia)) o marcar los jobs no entregados como 'sin IA' inmediatamente. En sweep(), si _sweep_lock está contendido (acquire(blocking=False)), abortar en vez de encolar el thread."},{"id":"OPS-4","severidad":"P1","titulo":"El batch nocturno run_ia_batch NO tiene breaker ni backoff: ante tormenta 429 martilla ollama-cloud toda la noche (secuencial, sleep 3s, sin stop_event)","linea":"enrich.py:426-472, 457-470, 225-252","evidencia":"run_ia_batch itera pending (enrich.py:457-470) llamando ia_extract → ia_extract_detail que reintenta cfg.ia.retries+1 veces con sleep(2) (enrich.py:225-252) y ante 429/5xx devuelve (None,'rate')… pero el loop del batch ignora err_kind (usa ia_extract, enrich.py:459, que descarta el tipo) y sigue a la siguiente oferta con time.sleep(3) (enrich.py:470). Sin ventana, sin contador, sin Event de parada: con la cola real observada (journal muestra 'IA 40..52/252' — 252 en cola) son 252 martillazos + reintentos por noche, y el límite de 40 por corrida (enrich.py:453, limit=cfg.ia.batch_size) solo lo recorta a 40. La spec §7 acepta rate limits desconocidos con 'IA_CONCURRENCY como knob, breaker como red, cola nocturna como red de la red' — no acepta que la propia red de seguridad carezca de breaker; el breaker B5 solo existe en consume_lote (cli.py:126-134). Evidencia de operación real: journal 01:45:53 'IA batch falló: HTTPSConnectionPool Read timed out (read timeout=120)' y corrida interrumpida por restart del servicio a las 02:00:53.","recomendacion":"Reusar el patrón B5 en run_ia_batch: contar 'rate' en ventana 20 y abortar la corrida (retornar done parcial) cuando tripee; backoff exponencial en vez de sleep(3) fijo."},{"id":"OPS-5","severidad":"P2","titulo":"Presupuesto de canal: descuenta SOLO posts reales enviados (correcto), pero presupuesto 0 salta el publish de los lotes siguientes silenciosamente (sin log ni phase)","linea":"cli.py:360, 364-366; channel.py:249, 263-264, 283-294","evidencia":"publish_channel descuenta presupuesto con ch_stats['posted'] (cli.py:365) y 'posted' solo incrementa cuando Telegram responde ok (channel.py:284-294, con commit por post en channel.py:293) — skipped/edad/dev-gate y envíos fallidos NO consumen presupuesto (channel.py:249 tope=min(max_posts,budget); break por tope en 263-264): correcto según A3. Pero cuando presupuesto_canal llega a 0, el guard cli.py:360 (and presupuesto_canal > 0) omite el publish del lote sin ningún log — solo el log.info de lotes anteriores deja rastro del presupuesto restante. Envíos fallidos (no-ok) quedan sin marcar y se reintentan en cada lote posterior del mismo barrido (hasta 7 intentos), con log.warning por intento (channel.py:298): ruido, no pérdida.","recomendacion":"Loguear (log.info/phase) cuando el publish de un lote se salta por presupuesto agotado; opcionalmente recordar los group_id fallidos del barrido para no reintentarlos en cada lote."},{"id":"OPS-6","severidad":"P2","titulo":"ia_queue_count usa el criterio viejo (desc>400) mientras run_ia_batch usa la cola relajada C9 (desc>200 OR description_source) — /enrich status y el ack del batch reportan una cola menor a la real","linea":"enrich.py:365-369 vs 446-454; bot.py:1009, 1031, 1077-1082","evidencia":"ia_queue_count: 'length(description)>400 AND (modality='' OR salary='' OR description IS NULL) AND ia_model=''' (enrich.py:368-369). run_ia_batch (C9 v4.1): '(length(description)>200 OR description_source!='') AND (modality='' OR salary='' OR description IS NULL)' (enrich.py:448-449). El mensaje de inicio del batch (bot.py:1013-1016), /enrich status (bot.py:1077-1082) y el resumen final (bot.py:1028-1036) usan ia_queue_count → cifras inconsistentes entre lo que se anuncia y lo que el batch realmente tomaría.","recomendacion":"Unificar el predicado (extraer la condición C9 a una constante/función compartida) o documentar que la cola mostrada es el subconjunto estricto."},{"id":"OPS-7","severidad":"P2","titulo":"Restart a mitad de lote (systemd Restart=on-failure): estado consistente sin corrupción, pero el barrido de esa hora se pierde y el presupuesto del canal se resetea (doble capacidad 15+15 mitigada por notified_channel_at)","linea":"cli.py:344, 298, 303; channel.py:285-293; bot.py:1419-1422; unidad systemd (Restart=on-failure, RestartSec=15)","evidencia":"Al morir el proceso: (1) resultados IA en memoria no commiteados se pierden → ofertas quedan sin IA y el batch nocturno las recoge (cola C9) — consistente; commit parcial cada 10/lote (cli.py:344). (2) channel_posts: el INSERT solo ocurre tras ok de Telegram con commit inmediato por post (channel.py:283-293) → no hay marcas fantasma; ventana residual mensaje-enviado-sin-marca entre sendMessage y commit (canal duplicaría ese post en el reintento) — es exactamente el riesgo residual que C3 mitiga pero no elimina; la spec §7 no lo lista pero el riesgo es menor (1 post). (3) presupuesto_canal se reinicializa a 15 en el nuevo proceso (cli.py:298) → entre dos corridas podrían publicarse hasta 30 en la ventana; mitigado porque notified_channel_at ya marca las enviadas (channel.py:285-286). (4) last_sweep_key ya fue seteado antes del crash (bot.py:1420) → al reiniciar el servicio dentro de la misma hora NO se re-dispara el barrido (bot.py:1376-1380) y el siguiente es 4h después. Journal: 3 restarts del servicio el 2026-09-04 (02:00:53, 03:00:52, 03:20:14) con batch IA en curso a las 02:00 — escenario ya ocurrido.","recomendacion":"Persistir last_sweep_key (o derivarlo de scan_log.ts) para poder reintentar el barrido interrumpido; aceptar documentadamente la ventana send-sin-marca o marcar ANTES del send y deshacer en fallo."},{"id":"OPS-8","severidad":"P2","titulo":"Contención DB verificada OK en el código nuevo: busy_timeout=60000 + WAL en connect, commit-por-ficha en enrich_pending libera el lock entre fichas, workers IA sin SQLite; los errores 'database is locked' del journal son del código previo","linea":"db.py:21-25; enrich.py:351, 309-353; cli.py:58-82; journal 00:37:03 y 00:53:51","evidencia":"database.connect: sqlite3.connect(timeout=60) + PRAGMA journal_mode=WAL + PRAGMA busy_timeout=60000 (db.py:21-25) — sí está puesto. enrich_pending: hace el fetch HTTP (extract_structured timeout 25s, o playwright AIRA ~30s) FUERA de la transacción y commitea por ficha (enrich.py:351) → el write lock se retiene ms por ficha; time.sleep(2) entre fichas sin lock (enrich.py:353). rescore_ids hace un solo commit al final (db.py:236) pero el UPDATE es local y acotado al lote — lock corto. worker_ia es HTTP-puro (cli.py:58-82, sin conn). Escritores externos: /stats, /jobs, callbacks, /channel-reset y purges abren su propia conn con el mismo busy_timeout (bot.py:384, 429, 483, 629, 823-828, 874). Los errores del journal ('batch IA falló: database is locked' 00:37:03; sqlite3.ProgrammingError cross-thread 00:53:51) provienen de procesos PIDs 379248/394010 anteriores al código actual (db.py mttime 02:41) — pendiente confirmar en el primer barrido agendado con el daemon nuevo (no verificable hoy sin esperar el barrido de las 04:00 UTC).","recomendacion":"Sin acción obligatoria; tras el primer barrido de 04:00 con el daemon nuevo, revisar journal buscando 'locked'/'ProgrammingError' para confirmar la mejora."},{"id":"OPS-9","severidad":"P2","titulo":"Los marcadores de digests/batch agendado se consumen ANTES de ejecutar: un fallo del digest lo pierde hasta el bucket siguiente (día/semana/mes)","linea":"bot.py:1243-1244, 1249-1251, 1255-1257, 1203","evidencia":"_digests_maybe marca keys[k]=1 y recién después llama publish_daily_digest/weekly/trends (bot.py:1243-1257): si el publish falla (Telegram caído, excepción atrapada en bot.py:1258-1259), el digest de ese día/semana no se reintenta en el próximo tick — contradice el espíritu C2 ('el digest corre en el próximo tick'). Igual en _ia_sweep_maybe: state['ia_log']['day'] se marca antes de lanzar el thread (bot.py:1203) — si el thread muere antes de _IA_STATE.running, el batch del día se pierde.","recomendacion":"Marcar la clave SOLO tras un publish exitoso (las funciones retornan bool) o revertirla en fallo."}],"resumen":"Auditoría QA/OPS de producción del pipeline v4.1 (escenarios de fallo, READ-ONLY). Verificado con código real (cli.py/enrich.py/db.py/channel.py/bot.py/config.py), 48 tests en verde (.venv/bin/python -m pytest → 48 passed, incluidos 12 de test_v41.py), DB real en modo ro y journalctl --user -u jobhunt. LO QUE ESTÁ BIEN: contratos núcleo implementados — worker HTTP-puro con resultado garantizado try/finally (cli.py:58-82), terminación por conteo con drain 120s acotado (cli.py:108-157), single-writer respetado (apply/rescore/enrich/publish solo en main), commit por post en publish_channel (channel.py:293), commit por ficha en enrich_pending (enrich.py:351), busy_timeout=60000+WAL (db.py:24), rescore_ids con aislamiento por fila y conservación de market_score (db.py:213-237), presupuesto por barrido descontando solo posts reales (cli.py:365), guardas C1/C2 presentes para /search y flag running crash-safe (finally en bot.py:576-577, sin riesgo de running=True pegado). LO QUE FALLA EN PRODUCCIÓN (4×P1, motivo del rechazo): (1) scan_log inserta ANTES del pipeline → los contadores v4.1 valen siempre 0 y en la DB real las columnas ni existen aún (C5 incumplido); (2) el barrido AGENDADO —el camino principal cada 4h— no setea _SEARCH_STATE['running'], así que las guardas C1/C2 no protegen el caso real: digests leen pool a medio procesar y el batch IA puede competir como segundo escritor; además last_sweep_key se consume antes del check → barridos de 4h se pierden silenciosamente; (3) el breaker B5 frena los workers pero no el reloj: lote con breaker quemado espera su deadline completo (~121 min con 40/conc2; 7 lotes ≈ 14.1 h calculados) y los barridos siguientes se apilan como threads bloqueados en _sweep_lock (sin guarda de solapamiento); (4) run_ia_batch (nocturno y /enrich) no tiene breaker ni backoff: ante tormenta 429 martilla toda la noche (252 en cola en la corrida real del journal, cortada por un restart del servicio a las 02:00). P2: salto silencioso del publish por presupuesto 0; ia_queue_count con criterio viejo (>400) vs cola C9 (>200) — /enrich status miente; ventana residual send-sin-marca en canal (duplicado de 1 post tras crash, mitigado por notified_channel_at) y reset del presupuesto al reiniciar; marcadores de digest/batch consumidos antes de ejecutar. Observabilidad: logging a journald + data/jobhunt.log (5MB×3) funciona y los errores IA/canal son diagnosticables (journal muestra timeouts 429/120s, database is locked, restarts); pero scan_log —la pieza pensada para diagnosticar sin reproducir— hoy no captura nada del pipeline (OPS-1), por lo que un incidente de lotes solo queda en el log textual. Restart a mitad de lote deja estado consistente (sin corrupción, sin phantom marks); lo pendiente de verificación en vivo (no verificado: comportamiento del primer barrido agendado 04:00 UTC con el daemon nuevo, y rates reales de ollama-cloud bajo tormenta) no cambia ninguna conclusión. Correcciones sugeridas pequeñas (<15 líneas c/u en OPS-1/2/4): mover INSERT de scan_log, setear el flag running en sweep(), breaker+backoff en run_ia_batch.","rol":"QA / OPS-PRODUCCIÓN"}